#include "main.h"

/**
 * main - test a function that prints a half of a string,
 * followed by a new line.
 *
 * Return: Always 0
 */

int main(void)
{
	char *str;

	str = "0123456789";
	puts_half(str);
	return (0);
}
